jar {
    manifest {
        attributes("Implementation-Title": project.name, "Implementation-Version": version, "Main-Class": "ru.kami.client.ChatWindow")
    }
}

dependencies {
    compile project(':network')
}
